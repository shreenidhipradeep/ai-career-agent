"use client"
export default function ApproveButton({ applicationId }: { applicationId: string }) {
  return <button onClick={() => alert("Approve logic comes on Day 19")}>Approve & Mark as Applied</button>
}
